import { callbackify } from "util";

// export const readKerasModel = async (modelPath, callback) => {
//     HTTP.get(Meteor.absoluteUrl(modelPath), function(err,result) {
//         callback(result);
//     }); 
// }

